services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-python_do}
      POSTGRES_USER: ${POSTGRES_USER:-python_do}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-python_do}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-python_do}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
